const facebookProfile = {
  name: "Vidurath Auchraje",
  hobbies: ["coding", "guitar", "sleeping"],
  memberSince: 2007,
};

// console.log(facebookProfile);

const jsonProfile = JSON.stringify(facebookProfile);

console.log(jsonProfile);
